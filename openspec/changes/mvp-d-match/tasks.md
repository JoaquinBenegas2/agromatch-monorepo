## 1. C4 · Explicador con control de alucinación (PR 1/4)

- [ ] 1.1 `packages/ai/src/lib/validate-numbers.ts`: `validateNumbers(text, facts)` extrae todos los números del texto (coma o punto decimal), compara contra cada valor numérico presente en `facts` (incluyendo anidados: `damTraits`, `expectedProgeny`, `deltaVsDam`, `compatibility`, `rank`, `totalCandidates`) con tolerancia de redondeo a 1 decimal. Verificar con Vitest: `validateNumbers("SCS 2,70", facts)` sin 2,7 en los hechos → `ok:false`; `validateNumbers("de 3,19 a 2.95", facts)` con esos valores → `ok:true` (REQ-D-04).
- [ ] 1.2 `packages/ai/src/lib/explainer.ts`: `AnthropicExplainer implements ExplainerPort`. Prompt en rioplatense, 3–4 oraciones, sin jerga, sin pedirle al LLM que calcule/estime/compare. Usa `LlmClient.completeText`. Si `validateNumbers` da `ok:false` o `llm-client` lanza (incluye `LlmUnavailableError`), devuelve `{ text: facts.reasons.join(' '), source: 'FALLBACK' }`, nunca propaga el error al controller. Verificar con Vitest (mock de `LlmClient`): número inventado → `FALLBACK`; LLM lanza → `FALLBACK` sin excepción (REQ-D-04, escenarios "número que no está" y "LLM no responde").
- [ ] 1.3 `packages/ai/src/index.ts`: exportar `AnthropicExplainer` y `validateNumbers`.
- [ ] 1.4 `apps/backend/src/ai/ai.providers.ts`: reemplazar la línea de `EXPLAINER_PORT` por el factory real (inyecta `LLM_CLIENT`), igual que `createLazyLiveLlmClient`. Verificar: `nx build backend` sin errores de tipos.
- [ ] 1.5 Verificación manual con `AI_MODE=live`: llamar `AnthropicExplainer.explain` con los hechos de `fixtures/samples/explanation-facts.json` (3031 × toro #1, `SOLIDS_CHEESE`) y confirmar `source:'AI'`, texto menciona mastitis, todos los números validan. Capturar la respuesta en la PR (REQ-D-04, criterio de aceptación).
- [ ] 1.6 Abrir PR `feature/C4-explicador` → `develop` con 1.1–1.3 en Vitest verde. Commits: `feat(ai): C4 validador de números y explicador con control de alucinación`.

## 2. B4 + D4 · Matching genético: endpoint + pantalla (PR 2/4)

- [ ] 2.1 `apps/backend/src/app/matching/matching.service.ts`: `getBoard(farmId, femaleId, goal)` — clasificación vigente o `DomainError('HERD_NOT_CLASSIFIED', 'Clasificá el rodeo antes de buscar toros', 409, { farmId, femaleId })` (REQ-D-02); `makeGeneticsNeed` + persistencia idempotente del Need sintético con id `` `synthetic:${farmId}:${femaleId}` `` y `synthetic:true` (REQ-D-03, decisión D3); proyección de toros a `Capability` filtrando por `semenTypes` permitidos por el tier; `computeTraitStats`; `matchNeed(need, caps, provs, [GeneticsVertical], ctx)` — nunca `scoreCandidates`/`scoreOneCandidate` directo (REQ-D-01).
- [ ] 2.2 `apps/backend/src/app/matching/matching.controller.ts`: `POST /farms/:farmId/females/:femaleId/matches` (`@UseGuards(FarmAccessGuard)`, body validado con `BreedingGoalSchema`) → `MatchBoard`. `apps/backend/src/app/matching/matching.module.ts`, agregar a `AppModule.imports`.
- [ ] 2.3 `apps/backend/src/app/matching/explanation.service.ts`: `getExplanation(farmId, femaleId, naab, goal)` reusa `getBoard` para ubicar el `MatchCandidate`, cachea con `InMemoryCache<Explanation>` de `packages/ai` keyeado por `cache.key(candidate.verticalFacts)`. `POST /farms/:farmId/females/:femaleId/matches/:naab/explanation` en el mismo controller.
- [ ] 2.4 Vitest de `matching.service`: spy sobre `matchNeed` — una llamada por `getBoard`, `need.category === 'GENETICS'` (REQ-D-01, criterio M3); hembra sin clasificar → `HERD_NOT_CLASSIFIED` (REQ-D-02); dos `getBoard` seguidas con la misma hembra+goal → mismo `needId` (REQ-D-03). Marcar `.skip('// mvp-a-core I2')` únicamente los asserts que dependan de un ranking/score real, no los de contrato/flujo.
- [ ] 2.5 Verificación manual API (capturar en la PR): `POST /matches` de hembra sin clasificar → 409 `HERD_NOT_CLASSIFIED`; log/spy de `matchNeed` con `category:'GENETICS'`; dos llamadas a `/explanation` → una sola invocación del `ExplainerPort` (REQ-D-05); `GET /needs?farmId=farm-a` después de 3 matches no lista sintéticos (REQ-D-03).
- [ ] 2.6 `apps/frontend/src/shared/api/hooks/use-match-board.ts` y `use-explanation.ts`: `useQuery` con `queryKeys.matches`/`queryKeys.explanation` ya definidos, `goalHash` = hash local de `goal` (decisión D6). `use-plan-item-mutations.ts` (solo `POST /plan/items` acá; `DELETE` se termina en la sección 4).
- [ ] 2.7 `apps/frontend/src/features/matching/`: `MatchingScreen` (ruta `/motor-genetico/matching/:femaleId?`), barra de objetivo (texto libre + `Select` de presets + "Subir Excel" que navega a `/motor-genetico/importar` + "Procesar"), ficha fija oscura de la hembra (`EmptyState` con selector si no hay `:femaleId`, REQ-D-08), `MatchRow` (badge `#k de N · {compatibility}` — **nunca `%`**, expandible, primera fila expandida por defecto con prefetch de la siguiente, REQ-D-06/Q3/Q4), sección de excluidos inline atenuada con motivo (REQ-D-09), los 4 estados obligatorios (`Skeleton`, `EmptyState`, `ErrorMessage`, vacío).
- [ ] 2.8 `apps/frontend/src/app/app.tsx`: agregar `<Route path="/motor-genetico/matching/:femaleId?" element={<MatchingScreen />} />` dentro del `<Route element={<AppShell />}>` (decisión D6). Verificar: `nx serve frontend` navega a la ruta y la ve real, no el placeholder; el resto de `/motor-genetico/*` (tablero, importar, asesor) sigue mostrando `NotImplementedPlaceholder`.
- [ ] 2.9 Verificación manual de pantalla (capturar en la PR): recorrido con `VITE_MOCKS=true` completo hasta "Elegir para el plan" (deja el POST real armado, aunque el botón termine de conectarse en la sección 4); todos los badges matchean `/^#\d+ de \d+ · \d+$/`, ninguno tiene `%`/"match"/"probabilidad" (REQ-D-06); hijos de `029HO19531` al final, atenuados, con motivo (REQ-D-09); contraer/expandir no repite el request de `/explanation` (Network tab); "Procesar" con texto vacío no llama a `/goals/parse` (usa el preset; el parser real se conecta en la sección 3, por ahora default a `GOAL_PRESETS`); "Subir Excel" navega a `/motor-genetico/importar`.
- [ ] 2.10 Abrir PR `feature/B4-D4-matching-genetico` → `develop`. Commits: `feat(backend): B4 endpoint de matching genético enchufado a matchNeed`, `feat(frontend): D4 pantalla Matching genético`.

## 3. C5 · Objetivo en lenguaje natural (PR 3/4)

- [ ] 3.1 `packages/ai/src/lib/goal-parser.ts`: `AnthropicGoalParser implements GoalParserPort`. `parse(text)` pide `BreedingGoal` estructurado por `completeJson` contra `BreedingGoalSchema`, conserva `rawText`, valida pesos en `[0,1]` y normaliza `weights` para que sumen 1 (nunca error). Verificar con Vitest: "leche A2 para vender a la industria" → `wantBetaA2:true`, `rawText` presente; `weights:{pro:.5,fat:.5,scs:.5}` → normalizado a `≈.333` c/u, suma `1±0.001` (REQ-D-15).
- [ ] 3.2 `apps/backend/src/app/planning/goals.controller.ts` (vive en `PlanningModule`, decisión D1): `POST /goals/parse` → `BreedingGoal`. Si `text` vacío, no llama al LLM (usa el preset del body o `BALANCED`) — mismo criterio que REQ-D-07 en el front.
- [ ] 3.3 `apps/backend/src/ai/ai.providers.ts`: reemplazar la línea de `GOAL_PARSER_PORT` por el factory real.
- [ ] 3.4 `apps/frontend/src/shared/api/hooks/use-goal-parse.ts` + conectar en `MatchingScreen`: "Procesar" llama a `/goals/parse` solo si el texto no está vacío (REQ-D-07), muestra los pesos resultantes editables, recalcula `useMatchBoard` con el nuevo `goal` sin cambiar de ruta.
- [ ] 3.5 Verificación manual (capturar en la PR): texto → pesos editables y lista recalculada sin cambiar de ruta; texto vacío + preset `BALANCED` → no se llama a `/goals/parse` (REQ-D-07, escenario "Procesar con el texto vacío").
- [ ] 3.6 Abrir PR `feature/C5-objetivo-lenguaje-natural` → `develop`. Commit: `feat: C5 objetivo en lenguaje natural, normaliza pesos sin fallar`.

## 4. B5 + D5 · Plan de servicios (PR 4/4)

- [ ] 4.1 `packages/genetics-core/src/lib/genetics-core.ts`: reemplazar el stub de `buildAutoPlan` — un `PlanItem` por hembra clasificada con `tier !== 'CULL_ALERT'`, toro `#1` de su `MatchBoard` (usa `scoreCandidates`, el único lugar permitido por la spec de `mvp-a-core`), sin restricción de presupuesto. Vitest: `farm-a` clasificado (2 en `CULL_ALERT`) → un ítem por hembra elegible, sin duplicados, `semenType` permitido por el tier (REQ-D-10).
- [ ] 4.2 `apps/backend/src/app/planning/plan.service.ts`: `getPlan`, `addItem`/`removeItem` (reemplaza/quita por `femaleId`, un toro por hembra, REQ-D-12), `autoFill` (pisa todos los ítems, Q5), `totals` calculado en el server — `doses` por `SemenType`, `cost` ignora `pricePerDose:null`, `avgExpectedProgeny` promedia solo ítems con perfil (REQ-D-11). Vitest: plan con precios `38, null, 12` → `totals.cost===50`, `totals.doses` suma 3.
- [ ] 4.3 `apps/backend/src/app/planning/plan.controller.ts`: `POST /farms/:farmId/plan/items`, `DELETE /farms/:farmId/plan/items/:femaleId`, `POST /farms/:farmId/plan/auto`, `GET /farms/:farmId/plan` — todos con `FarmAccessGuard`.
- [ ] 4.4 `apps/backend/src/app/planning/plan-export.controller.ts` (o método del mismo controller): `GET /farms/:farmId/plan/export.csv` — columnas `visualId, tier, toro, central, tipoSemen, compatibilidad, precio`, BOM UTF-8 (`﻿` al inicio), `Content-Type: text/csv; charset=utf-8`. Verificar con Vitest que el buffer empieza con el BOM y el header tiene las 7 columnas en orden (REQ-D-13).
- [ ] 4.5 `apps/backend/src/app/planning/planning.module.ts` con `MatchingController`/`PlanController`/`GoalsController`/servicios; agregar a `AppModule.imports`.
- [ ] 4.6 `apps/frontend/src/shared/api/hooks/use-plan.ts`, completar `use-plan-item-mutations.ts` (agregar `useRemovePlanItem`, `useAutoPlan`) con `queryKeys.plan`.
- [ ] 4.7 `apps/frontend/src/features/plan/`: `PlanScreen` (ruta `/negociacion/plan`) — tabla de ítems (`visualId`, tier, toro, central, tipo de semen, `#k de N · compatibilidad`, precio), `StatCard`s de totales, botón "Plan automático" (pide el objetivo vigente, Q2), botón "Exportar CSV" (dispara la descarga del endpoint), los 4 estados obligatorios, clic en una fila navega a `/motor-genetico/matching/:femaleId` con el toro marcado "En el plan" (REQ-D-14).
- [ ] 4.8 Completar en `features/matching/MatchRow`: acción primaria "Elegir para el plan" (nunca "Iniciar trato", N8) → `POST /plan/items`; una vez elegido, `Badge variant="ok"` "En el plan" y la acción pasa a "Quitar del plan" → `DELETE` (REQ-D-08).
- [ ] 4.9 `apps/frontend/src/app/app.tsx`: agregar `<Route path="/negociacion/plan" element={<PlanScreen />} />`.
- [ ] 4.10 Verificación manual (capturar en la PR): recorrido completo Tablero → 3031 → "sólidos" + Procesar → "Elegir para el plan" → "En el plan" → aparece en `/negociacion/plan` (REQ-D-08, escenario completo); CSV descargado abre en Excel con tildes correctas (REQ-D-13); los 4 estados en `/negociacion/plan`; plan vacío muestra `EmptyState` con "Plan automático" y enlace a matching (REQ-D-14).
- [ ] 4.11 Abrir PR `feature/B5-D5-plan-servicios` → `develop`. Commits: `feat(genetics-core): B5 buildAutoPlan real, mejor toro por hembra`, `feat: B5+D5 endpoints y pantalla de plan de servicios`.

## 5. Cierre de la ruta

- [ ] 5.1 Confirmar que el sidebar no tiene ítems "Swipe" ni "Plan" propios (son tabs de "Motor genético" y "Negociación y tratos", `frontend-shell` REQ-FS-01) — revisión visual, sin cambios de código esperados.
- [ ] 5.2 Actualizar Notion (`torinder-notion-sync`): marcar C4, B4, D4, C5, B5, D5 como cerradas y publicar el contrato final de `MatchBoard`/`BreedingPlan` tal como quedaron (sin cambios respecto a `contracts-v1`, según proposal.md).
- [ ] 5.3 Si para I2 `mvp-a-core` ya reemplazó los stubs de `matching-core`/`genetics-core`, sacar los `.skip` de 2.4 y correr toda la suite de la ruta una vez más antes del congelamiento (hora 16).
