import { Controller, Get, Param, UseGuards } from '@nestjs/common'; import { FarmAccessGuard } from '../auth/farm-access.guard.js'; import { HerdService } from './herd.service.js'; @Controller('farms/:farmId/females') @UseGuards(FarmAccessGuard) export class HerdController { constructor(private readonly service: HerdService) {} @Get() list(@Param('farmId') farmId: string) { return this.service.list(farmId); } }
