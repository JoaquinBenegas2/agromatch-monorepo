import { ClassificationService } from './classification.service.js';
import type { Farm, Female } from '@org/shared-types';
const farm: Farm = { id: 'farm', name: 'Farm', location: 'AR', tierQuotas: { sexedPct: 25, beefPct: 30 }, calvingEaseMaxHeifer: 3, scsGrayZone: { from: 3.1, to: 3.18 }, plGrayZone: { from: 0, to: .2 } };
const female: Female = { id: 'f', farmId: 'farm', visualId: '1', birthDate: '2024-01-01', sireNaab: 'sire', category: 'HEIFER', profile: { traits: { ci: 500, milk: 1, fat: 1, pro: 1, pl: 1, scs: 3, fs: 1, rfi: 1 }, betaCasein: null, kappaCasein: null, scale: 'CDCB', source: 'test' } };
describe('ClassificationService', () => { it('replaces the classification and produces a summary', async () => { const repo = { replaceForFarm: jest.fn(), listByFarm: jest.fn().mockResolvedValue(null) }; const service = new ClassificationService({ listByFarm: jest.fn().mockResolvedValue([female]) } as never, { findById: jest.fn().mockResolvedValue(farm) } as never, repo as never); await service.classify('farm', { preset: 'BALANCED', weights: {}, wantBetaA2: false, wantKappaBB: false }); expect(repo.replaceForFarm).toHaveBeenCalled(); }); it('rejects a herd without profiles', async () => { const service = new ClassificationService({ listByFarm: jest.fn().mockResolvedValue([{ ...female, profile: null }]) } as never, { findById: jest.fn().mockResolvedValue(farm) } as never, {} as never); await expect(service.classify('farm', { preset: 'BALANCED', weights: {}, wantBetaA2: false, wantKappaBB: false })).rejects.toMatchObject({ code: 'HERD_EMPTY' }); }); });
