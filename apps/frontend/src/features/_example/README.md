# features/_example

Patrón de referencia para una pantalla nueva (REQ-FS-06): copiá `example-bull-list.tsx` y reemplazá el hook de React Query y el render de "con datos" por los de tu feature. Los cuatro estados (cargando/error/vacío/con datos) y los componentes (`Skeleton`, `ErrorMessage`, `EmptyState`) se mantienen iguales.
