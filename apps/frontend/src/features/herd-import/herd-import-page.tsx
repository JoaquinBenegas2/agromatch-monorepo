import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import type { ColumnMapping, MappingProposal } from '@org/shared-types'; import { Button } from '@/components/ui/button'; import { ErrorMessage } from '@/components/ui/error-message'; import { useConfirmHerd, useUploadHerd } from '../../shared/api/hooks/use-herd.js';
export function UploadHerdButton() { const navigate = useNavigate(); return <Button variant="secondary" onClick={() => navigate('/motor-genetico/importar')}>Subir Excel</Button>; }
export function HerdImportPage({ farmId }: { farmId: string }) { const upload = useUploadHerd(farmId), confirm = useConfirmHerd(farmId), navigate = useNavigate(); const [proposal, setProposal] = useState<MappingProposal | null>(null); const [importId, setImportId] = useState(''); const [reviewed, setReviewed] = useState<string[]>([]); const low = Object.entries(proposal?.confidence ?? {}).filter(([, value]) => value < .8).map(([header]) => header); const submit = async (file: File) => { const result = await upload.mutateAsync(file); setProposal(result.proposal); setImportId(result.importId); };
  if (confirm.data) return <section><h1>Rodeo importado</h1><p>{confirm.data.rowsOk} filas importadas</p><Button onClick={() => navigate('/motor-genetico/tablero')}>Ir al tablero</Button></section>;
  return <section><h1>Carga del rodeo</h1><div role="button" tabIndex={0} onDragOver={(event) => event.preventDefault()} onDrop={(event) => { event.preventDefault(); const file = event.dataTransfer.files[0]; if (file && /\.xlsx?$/i.test(file.name)) void submit(file); }}><p>Soltá aquí tu Excel o elegilo desde tu equipo.</p><input aria-label="Archivo Excel" type="file" accept=".xls,.xlsx" onChange={(event) => { const file = event.target.files?.[0]; if (file) void submit(file); }} /></div>{(upload.error || confirm.error) && <ErrorMessage message={((upload.error ?? confirm.error) as Error).message} />}{proposal && <><h2>Revisá el mapeo</h2><table><tbody>{Object.entries(proposal.columns).map(([header, target]) => <tr key={header}><td>{header}</td><td><select value={target} onChange={() => setReviewed((current) => [...new Set([...current, header])])}><option>{target}</option></select></td><td>{proposal.confidence[header]?.toFixed(2)}</td></tr>)}</tbody></table><Button disabled={low.some((header) => !reviewed.includes(header)) || confirm.isPending} onClick={() => void confirm.mutateAsync({ importId, mapping: { headerRow: proposal.headerRow, columns: proposal.columns } as ColumnMapping })}>Confirmar</Button></>}</section>; }
